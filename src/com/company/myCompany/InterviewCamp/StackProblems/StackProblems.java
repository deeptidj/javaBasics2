package com.company.myCompany.InterviewCamp.StackProblems;

/*
Last InFirst Out
Implemented using a Linked List. 1 -> 2 -> 3
Restrict additions and removals from one side.
Adding an entry is called push. Removing an entry is called pop.
Peek is when you look at the top element.
If you try to remove from an empty stack, it is called stack underflow.
If you try to add entry when stack is full, it is called stack overflow.
Lookup time for value stored in stack - O(n)
Traverse through a stack ? Use a temporary stack
Call stack is when you oput each function call in a recursive function.
s = | 3 | 2 | 8 |
 */
public class StackProblems {
}
